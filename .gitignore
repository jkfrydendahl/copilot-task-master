# Local cost-audit log written by Start-CopilotWork.ps1 (personal, churns often)
usage-log.csv

# Pending session marker (written at launch, removed on graceful exit)
usage-pending.json

# Seen-models cache for the new-model nudge (local state)
.known-models.json

# Local machine repo paths (personal, not portable)
repos.json
